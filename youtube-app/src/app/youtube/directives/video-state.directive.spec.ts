import { VideoStateDirective } from './video-state.directive';

describe('VideoStateDirective', () => {
  it('should create an instance', () => {
    const directive = new VideoStateDirective();
    expect(directive).toBeTruthy();
  });
});

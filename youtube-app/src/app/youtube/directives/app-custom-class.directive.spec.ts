import { AppCustomClassDirective } from './app-custom-class.directive';

describe('AppCustomClassDirective', () => {
  it('should create an instance', () => {
    const directive = new AppCustomClassDirective();
    expect(directive).toBeTruthy();
  });
});

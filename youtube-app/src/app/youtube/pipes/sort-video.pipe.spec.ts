import { SortVideoPipe } from './sort-video.pipe';

describe('SortVideoPipe', () => {
  it('create an instance', () => {
    const pipe = new SortVideoPipe();
    expect(pipe).toBeTruthy();
  });
});
